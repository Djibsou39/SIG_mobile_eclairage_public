-- Table: public.pl

-- DROP TABLE IF EXISTS public.pl;

CREATE TABLE IF NOT EXISTS public.pl
(
    gid integer NOT NULL DEFAULT nextval('pl_gid_seq'::regclass),
    geom geometry(Point,2154),
    id bigint,
    codecoffret character varying(9) COLLATE pg_catalog."default",
	codecable character varying(10) COLLATE pg_catalog."default",
    datereleve character varying(29) COLLATE pg_catalog."default",
    typepl integer COLLATE pg_catalog."default",
	materiau integer COLLATE pg_catalog."default",
	nblampes integer COLLATE pg_catalog."default",
	etatpl integer COLLATE pg_catalog."default",
    remarque character varying(109) COLLATE pg_catalog."default",
    rue character varying(13) COLLATE pg_catalog."default",
	nomrue character varying(32) COLLATE pg_catalog."default",
	photo character varying(32) COLLATE pg_catalog."default",
	lienstreetview character varying(32) COLLATE pg_catalog."default",
    CONSTRAINT coffret_pkey PRIMARY KEY (gid)
)

TABLESPACE pg_default;

ALTER TABLE IF EXISTS public.pl
    OWNER to kvukrw_pqista;
-- Index: sidx_pl_geom

-- DROP INDEX IF EXISTS public.sidx_pl_geom;

CREATE INDEX IF NOT EXISTS sidx_pl_geom
    ON public.pl USING gist
    (geom)
    TABLESPACE pg_default;