-- Ajoutez une colonne idpl dans la table photo
ALTER TABLE public.photo
ADD COLUMN idpl bigint;
ALTER TABLE public.photo DROP constraint fk_photo_idpl;
ALTER TABLE public.photo DROP constraint fk_photo_idpl_not_null;
-- Créez la contrainte de clé étrangère
ALTER TABLE public.photo
ADD CONSTRAINT fk_photo_idpl
FOREIGN KEY (idpl)
REFERENCES public.pl(gid) 
ON DELETE CASCADE;

-- Vous pouvez également ajouter une contrainte d'intégrité pour vous assurer qu'une photo ne peut pas exister sans une référence à une entrée dans la table pl
ALTER TABLE public.photo
ADD CONSTRAINT fk_photo_idpl_not_null
CHECK (idpl IS NOT NULL);